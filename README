# 社交点菜小程序
一个面向个人的“私房菜”社交点菜工具。每个用户将自家主页公开给访客挑选菜品，系统自动对比库存生成缺失食材清单，方便访客自带。核心功能与数据对象详见《PRODUCT_REQUIREMENTS.md》，技术方案与后端 API 草案见《SYSTEM_DESIGN.md》，实施拆解与里程碑规划见《IMPLEMENTATION_PLAN.md》。

## 本地快速启动（M0 脚手架）
1. 安装依赖：`npm install`（当前依赖全部内置，无需访问外网）
2. 复制并修改环境变量：`cp .env.example .env`（至少设置 `JWT_SECRET`）
3. 编译：`npm run build`
4. 启动服务：`npm start`
5. 健康检查：`GET http://localhost:3000/api/healthz`

### 可用接口（内存存储，仅示范鉴权流程）
- `POST /api/auth/register`：`{"name","email","password"}` 创建用户并返回 JWT
- `POST /api/auth/login`：`{"email","password"}` 登录并返回 JWT
- `GET /api/me`：携带 `Authorization: Bearer <token>` 获取当前用户（示范鉴权守卫）
- `POST /api/dishes`：创建菜品（需要 `title`、`coverUrl`、`ingredients[]`）
- `GET /api/dishes` / `GET /api/dishes/:id`：浏览菜单
- `PUT /api/dishes/:id`：主人更新菜品
- `PUT /api/pantry` / `GET /api/pantry`：主人维护库存（有/缺/快用完）
- `POST /api/orders`：访客按菜品下单，自动生成缺失食材并可声明自带
- `GET /api/orders`：访客/主人查看自己相关的订单
- `PATCH /api/orders/:id/status`：主人/访客更新订单状态（取消/接受/拒绝/建议修改/完成）

> 当前为无数据库的演示骨架，用于后续接入 PostgreSQL/Prisma、订单与缺失食材计算服务。
