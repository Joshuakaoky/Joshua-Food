# 社交点菜小程序产品需求书

## 产品愿景
面向个人的“私房菜”社交小程序。每位用户把自己的家作为可访问的主页，展示拿手菜，让好友或访客点菜。系统自动对比主人库存与菜品食材清单，生成“缺失食材”供访客自备，全程无聊天，仅依靠点菜备注和主人回复备注完成沟通。

## 目标用户与场景
- **家庭厨师**：乐于分享或以“私房菜”形式招待朋友，需要简单的菜单与库存管理工具。
- **访客/好友**：希望到访前先选菜并确认所需自带食材，减少沟通成本。

## 核心功能模块
### 用户主页（我的家 / TA 的家）
- 展示：头像、昵称、简介、菜单（菜品列表）。
- 访问权限：公开 / 私密（至少支持这两种，预留“仅好友”）。
- 访客入口：一键“点菜”按钮，可跳转到点菜流程。

### 菜品（Menu Items）
- 主人：新增 / 编辑 / 下架菜品。
- 字段：
  - 必填：菜名、封面图、食材清单（食材名称；数量/单位可选）。
  - 可选：图集、简介/做法亮点、标签（辣/清淡/素/下酒菜/快手等）。
- 访客：浏览菜品详情，加入“本次想点的菜”清单。

### 库存（Pantry）
- 主人维护家里现有食材，用于缺失食材计算。
- 字段：食材名称（必填），状态（有/缺/快用完，可选），数量/单位（可选，MVP 可不算数量）。
- 以“有没有”为核心，非精细库存。

### 点菜请求（Order Request，无消息）
- 访客发起：
  - 选择菜品（可多选）。
  - 人数（可选）。
  - 期望到访时间（可选）。
  - 访客备注（单段文字）。
  - 自动计算的缺失食材清单，访客可勾选“我自带”生成自带清单。
- 主人处理：
  - 操作：接受 / 拒绝 / 建议修改。
  - 主人回复备注：用于表达调整（如改期、让访客自带食材）。
- 无聊天，仅依赖备注。

## 缺失食材计算
- 每道菜：`missing = required_ingredients - pantry_has`，按**食材名称**匹配，库存仅取状态=有。
- 多菜合并：同名食材去重（MVP 不做数量累加）。
- 结果用于访客查看与“自带”勾选。

## 订单状态流转
- `PENDING` 待处理：访客提交。
- `ACCEPTED` 已接受。
- `REJECTED` 已拒绝。
- `REVISION_REQUESTED` 建议修改：主人附回复备注。
- `CANCELLED` 已取消：访客或主人可取消。
- `COMPLETED` 已完成：主人手动完成。
- 无聊天前提下，建议修改后需访客重新提交或取消。

## 页面/流程（MVP）
1. 登录/注册。
2. 首页（可简化为入口导航）。
3. TA 的家主页（菜单列表 + 点菜按钮）。
4. 菜品详情页（展示标签、食材、亮点）。
5. 发起点菜页（选菜 -> 缺失食材 -> 自带勾选 -> 备注 -> 提交）。
6. 我的订单（访客视角：我发出的请求）。
7. 接单台（主人视角：我收到的请求）。
8. 菜品管理（新增/编辑/下架）。
9. 库存管理（标记有/缺/快用完）。
10. 设置：主页可见性。

## 关键数据对象
- **User**：`id, name, avatar, bio, visibility`（公开/仅好友/私密）。
- **Dish**：`id, owner_id, title, cover, photos[], intro, tags[], ingredients[]`。
- **Ingredient**：`name, qty?, unit?`（MVP 可空）。
- **PantryItem**：`owner_id, name, status, qty?, unit?`。
- **Order**：`id, host_id, guest_id, dish_ids[], guest_note, host_note, visit_time?, people_count?, missing_ingredients[], bring_by_guest[], status, created_at`。

## 交互与提示
- 订单列表需展示状态与主人回复备注，方便访客一眼看到处理结果。
- “缺失食材”与“访客自带”清单要突出展示，支持导出/复制。
- “建议修改”场景提供“按建议重新提交”快捷入口，减少重复填写。

## 后续扩展思考（非 MVP）
- 食材别名映射、数量累加、口味偏好标签、好友关系与仅好友可见。
- 提醒通知（处理结果、改期提醒）。
- 简易评分/收藏，帮助访客筛选菜品。
